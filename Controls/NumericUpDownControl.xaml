<UserControl
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Button x:Name="UpButton" Grid.Column="1">^</Button>
        <Button x:Name="DownButton" Grid.Column="1" Grid.Row="1">v</Button>
        <TextBox x:Name="Input" Grid.RowSpan="2" />
        <!--If you Bind from XAML, Binding.OfExpression in NumericUpDownView::SetBindings is not needed-->
        <!--<TextBox x:Name="input" Grid.RowSpan="2" Text="{Binding Value}" /> -->
    </Grid>
</UserControl>